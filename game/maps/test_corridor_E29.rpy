
label test_corridor_E29_sign:
    $ pink.otm.start_continuing_event()
    """The pink engine not only supports having maps with different tile sizes in the same game, but it also supports
    maps with non-square tiles.

    This room is an example of such, using 64x48 tiles to create a foreshortening effect.
    """
    $ pink.otm.end_current_event()

