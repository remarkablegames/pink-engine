label test_corridor_B05_sign1:
    $ pink.otm.start_continuing_event()
    """This room exists to test parallel processes. The further up you go, the louder the music should get."""
    $ pink.otm.end_current_event()
