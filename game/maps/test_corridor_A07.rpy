label test_corridor_A07_sign:
    $ pink.otm.start_dynamic_event()
    """This room exists to test interactions with empty tiles. A lot of the tiles in this room have not been
    set. You should not be able to walk onto the empty tiles."""
    $ pink.otm.end_current_event()
