
label test_corridor_E25_sign:
    $ pink.otm.start_continuing_event()
    """This room exists to test having multiple sound emitting objects on the same map. Each of the computers
    in this room is a locational sound emitter.
    """
    $ pink.otm.end_current_event()

