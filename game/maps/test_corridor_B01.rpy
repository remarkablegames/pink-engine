label test_corridor_B01_sign1:
    $ pink.otm.start_continuing_event()
    """This room exists to test map properties. In the case of this particular room, it tests the forbid_run property.
     You should not be able to run in this room."""
    $ pink.otm.end_current_event()
