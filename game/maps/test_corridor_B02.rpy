label test_corridor_B02_sign:
    $ pink.otm.start_continuing_event()
    """This room exists to test map properties. In the case of this particular room, it tests the force_run property.
     You should not be able to do anything but run in this room."""
    $ pink.otm.end_current_event()
