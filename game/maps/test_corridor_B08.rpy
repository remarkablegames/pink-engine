label test_corridor_B08_sign1:
    $ pink.otm.start_continuing_event()
    """This room exists to test parallel processes. As you move across the room, the camera should switch between
    various locations and zoom levels."""
    $ pink.otm.end_current_event()
