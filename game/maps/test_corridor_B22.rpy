label test_corridor_B22_sign:
    $ pink.otm.start_continuing_event()
    """
    As of version 0.14.0, the Pink Engine can now affect multiple channels using a singular parallel process. In this
    room, the contents of both the 'music' and 'sound' channels changes depending on your position.
    """
    $ pink.otm.end_current_event()
