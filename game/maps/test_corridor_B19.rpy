label test_corridor_B19_sign:
    $ pink.otm.start_continuing_event()
    """This room exists to test transitions. Each door in this room has a different set of transitions attached to it."""
    $ pink.otm.end_current_event()
